<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>

{{!-- Yes, that's right—only one expression had to change.
Because "this" is being looped over, all of the expressions within the loop look for properties on each object in this. 
In this case, that means each comment. --}}